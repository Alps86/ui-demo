{
  "name": "ui-demo",
  "version": "0.0.1",
  "description": "Webcomponent ui-demo following open-wc recommendations",
  "scripts": {
    "build": "tsc",
    "prepublish": "tsc",
    "storybook": "tsc && concurrently -k -r \"tsc --watch --preserveWatchOutput\" \"wds -c .storybook/server.mjs\"",
    "storybook:build": "tsc && build-storybook"
  },
  "dependencies": {
    "lit": "^2.0.0-rc.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.17",
    "concurrently": "^5.3.0",
    "typescript": "^4.3.2",
    "tslib": "^2.2.0",
    "@web/dev-server-storybook": "^0.3.5",
    "@storybook/web-components": "^6.2.9"
  }
}
